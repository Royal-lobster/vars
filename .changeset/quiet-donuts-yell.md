---
"@dotvars/core": patch
"dotvars": patch
---

Preserve existing generated platforms when running `vars gen` without `--platform`, and emit serverless generated code that typechecks under strict indexed access settings.
