---
title: Framework Integration
description: vars works with any framework — wrap your scripts and go.
icon: Blocks
---

## How it works

Wrap your dev and build scripts with `vars run`:

```json
{
  "scripts": {
    "dev": "vars run --env dev -- next dev",
    "build": "vars run --env prod -- next build"
  }
}
```

`vars run` decrypts your config, validates schemas, and injects variables into `process.env` before your framework starts. No plugins, no adapters.

## Server vs Client

**Server-side** (API routes, SSR, Node.js): `process.env` works directly. Import `vars` from `#vars` for type safety.

**Client-side** (browser bundles): Frameworks don't pass `process.env` to the browser. Two options:

1. **Static generation** — bake values into the generated file at build time:
   ```bash
   VARS_KEY=$KEY vars gen config.vars --platform static --env dev
   ```

2. **Framework bridge** — use your framework's native env passthrough (details in each framework guide below)

## Choose your framework

import { Globe, Zap, Flame, Star, RefreshCw, Server, Cloud } from 'lucide-react';

<Cards>
  <Card icon={<Globe />} href="/docs/frameworks/nextjs" title="Next.js">
    Client vars work out of the box via NEXT_PUBLIC_*
  </Card>
  <Card icon={<Zap />} href="/docs/frameworks/vite" title="Vite / React">
    Needs static gen or vite.config.ts define bridge for client
  </Card>
  <Card icon={<Flame />} href="/docs/frameworks/sveltekit" title="SvelteKit">
    Server routes work, client needs bridge
  </Card>
  <Card icon={<Star />} href="/docs/frameworks/astro" title="Astro">
    Server islands work, client islands need static gen
  </Card>
  <Card icon={<RefreshCw />} href="/docs/frameworks/remix" title="Remix">
    Server-only — no client concerns
  </Card>
  <Card icon={<Server />} href="/docs/frameworks/express" title="Express / Hono / Fastify">
    Pure server, just works
  </Card>
  <Card icon={<Cloud />} href="/docs/frameworks/cloudflare" title="Cloudflare Workers">
    Use --platform cloudflare for env bindings
  </Card>
</Cards>
