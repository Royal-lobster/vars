---
title: CLI Workflow
description: All vars commands at a glance.
icon: Terminal
---

## Commands

| Command | Description |
|---------|-------------|
| `vars init` | Initialize vars in your project — creates config, key, gitignore, hook |
| `vars gen <file>` | Generate TypeScript types from a .vars file. `--platform node \| serverless \| deno \| static` selects the output shape. `cloudflare` was removed in the current major — use `serverless` instead. |
| `vars show [file]` | Decrypt a .vars file in-place for editing |
| `vars hide` | Encrypt all unlocked .vars files |
| `vars run --env <env> -- cmd` | Run a command with decrypted env vars injected |
| `vars check [-f file]` | Validate schemas and run check blocks |
| `vars add <name>` | Interactively add a variable |
| `vars remove <name>` | Remove a variable from a .vars file |
| `vars ls` | List all .vars files with lock state and var count |
| `vars ls <file>` | List variables in a file with metadata |
| `vars export --env <env>` | Export as dotenv, json, or k8s-secret format |
| `vars key init` | Create a new encryption key |
| `vars key rotate` | Rotate the encryption key |
| `vars key fingerprint` | Print the key fingerprint |
| `vars key export` | Print base64 master key for CI |
| `vars doctor` | Diagnose your vars setup |

## Environment

| Variable | Description |
|----------|-------------|
| `VARS_KEY` | Base64-encoded master key — bypasses PIN prompt. Required for CI/CD. Get it with `vars key export`. |
| `VARS_PIN` | PIN for trusted non-interactive use. Overridden by `--pin`; may expose all secrets available to that PIN. |

## Trusted automation

Commands that set or unlock a PIN accept `--pin <pin>`:

```bash
vars init --pin "$PIN"
vars show config.vars --pin "$PIN"
vars hide --pin "$PIN"
vars run --env dev --pin "$PIN" -- npm start
```

This bypasses the interactive prompt so a trusted coding agent or automation process can initialize, edit, encrypt, and run a project. Once an agent receives a PIN, it can decrypt every secret available to that PIN.

<Callout type="warn">Command-line arguments can appear in process listings, shell history, agent transcripts, and command logs. Prefer an owner-scoped PIN when possible. Use `VARS_KEY` for CI/CD.</Callout>

## Guides

- [Setup & Auth](/docs/cli/setup-auth) — init, key management, doctor
- [Managing Variables](/docs/cli/managing-variables) — show, hide, add, remove, ls
- [Running Apps](/docs/cli/running-apps) — run, gen, check, export, platform targets
