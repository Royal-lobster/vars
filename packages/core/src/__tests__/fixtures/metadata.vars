env(dev, prod)

API_KEY : z.string().min(32) {
  dev  = "sk_test_placeholder_32_chars_long!"
  prod = enc:v2:aes256gcm-det:abc:def:ghi
} (
  description = "Primary API key"
  owner = "backend-team"
  expires = 2026-09-01
  tags = [auth, critical]
)
