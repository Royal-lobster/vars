# This file has intentional schema errors for testing diagnostics

BAD_SCHEMA  z.notARealType()
  @dev = hello

MISSING_PAREN  z.string(.min(5)
  @dev = hello

VALID_ONE  z.string()
  @dev = works

BAD_CHAIN  z.number().fakeMethod()
  @dev = 42
