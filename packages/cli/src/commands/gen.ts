import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "gen",
    description: "Generate typed accessors from .vars",
  },
  run() {
    console.log("vars gen — not yet implemented");
  },
});
