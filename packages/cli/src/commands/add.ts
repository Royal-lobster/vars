import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "add",
    description: "Add a new variable with type, values per env, auto-encrypt",
  },
  run() {
    console.log("vars add — not yet implemented");
  },
});
