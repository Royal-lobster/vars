import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "show",
    description: "Decrypt all values in-place within .vars for editing",
  },
  run() {
    console.log("vars show — not yet implemented");
  },
});
