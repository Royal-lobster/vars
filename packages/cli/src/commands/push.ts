import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "push",
    description: "Push decrypted vars to a deployment platform",
  },
  run() {
    console.log("vars push — not yet implemented");
  },
});
