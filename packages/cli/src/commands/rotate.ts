import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "rotate",
    description: "Generate new key + PIN, re-encrypt all values",
  },
  run() {
    console.log("vars rotate — not yet implemented");
  },
});
