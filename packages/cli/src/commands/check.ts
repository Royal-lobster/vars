import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "check",
    description: "Validate all values against schemas",
  },
  run() {
    console.log("vars check — not yet implemented");
  },
});
