import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "pull",
    description: "Pull current vars from a platform, encrypt, update .vars",
  },
  run() {
    console.log("vars pull — not yet implemented");
  },
});
