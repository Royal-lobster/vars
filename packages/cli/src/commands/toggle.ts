import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "toggle",
    description: "Flip between show/hide states",
  },
  run() {
    console.log("vars toggle — not yet implemented");
  },
});
