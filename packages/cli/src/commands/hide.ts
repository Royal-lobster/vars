import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "hide",
    description: "Re-encrypt all values in-place within .vars",
  },
  run() {
    console.log("vars hide — not yet implemented");
  },
});
