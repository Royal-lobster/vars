import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "remove",
    description: "Remove a variable from .vars",
  },
  run() {
    console.log("vars remove — not yet implemented");
  },
});
