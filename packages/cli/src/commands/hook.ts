import { defineCommand } from "citty";

export default defineCommand({
  meta: {
    name: "hook",
    description: "Install git pre-commit hook for auto-encryption",
  },
  run() {
    console.log("vars hook — not yet implemented");
  },
});
